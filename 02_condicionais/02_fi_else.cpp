#include <iostream>

int main() {
    // instruções if simples com else
    int i = 2;
    if (i > 2) {
        std::cout << i << "é maior que 2 \n";
    } else {
        std::cout << i << "não é maior que 2 \n";
    }
    return 0;
}